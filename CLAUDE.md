# Trophy Room

Developer activity tracking via check-in files.

## Structure

`checkins/` - Individual JSON check-in records (hash-named)

Data repository only. No build/test commands - managed via check-in system interface.
